apiVersion: v1
kind: Service
metadata:
  name: spring-svc
  namespace: default
  labels:
    app: spring-svc
    app.kubernetes.io/managed-by: grad-deploy
spec:
  type: ClusterIP
  selector:
    app: spring-svc
  ports:
    - port: 8080
      targetPort: 8080