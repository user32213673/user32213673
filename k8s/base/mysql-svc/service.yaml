apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: default
  labels:
    app: mysql-svc
    app.kubernetes.io/managed-by: grad-deploy
spec:
  clusterIP: None
  selector:
    app: mysql-svc
  ports:
    - port: 3306
      targetPort: 3306